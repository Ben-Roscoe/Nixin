#ifndef _NIXIN_KEYBOARD_H_
#define _NIXIN_KEYBOARD_H_

#include <stdint.h>
#include <limits>
#include <QtGui>

namespace Nixin
{




	class Keyboard
	{


	public:



        void						Initialise();												// Initialise the keyboard.
        void						SetKey( QKeyEvent* e );                                     // Sets a key's state.
        bool						IsKeyDown( int key );                                       // Checks if a certain key is down.
        bool						IsKeyUp( int key );                                         // Checks if a certain key is up.
        bool						IsAnyKeyDown();												// Checks if any key is down.



    private:

        std::map<int, bool>             keys;
        int                             inputs;														// Number of keys being pressed.



	};
}


#endif
















































































































































































































































































































































































































































































































































































































































































